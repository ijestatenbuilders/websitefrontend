# Local development only (used by `npm start`).
# Talk to the local Django backend so locally-added properties show up.
# Production is unaffected — it uses .env.production.
REACT_APP_API_URL=http://localhost:8000
